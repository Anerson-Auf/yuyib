# Assets (local only)

Файлы `.glb` и `.yasset` в этой папке **не входят в Git**. Положите копии
локально (часто рядом из `for_tests/`). Лицензии third-party моделей на
публичный remote не заливаются.
